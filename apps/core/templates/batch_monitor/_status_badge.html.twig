{# Generated by GitHub Copilot #}
{% set meta = statusPresentation[status] ?? statusPresentation.pending %}

<span class="batch-status batch-status--{{ meta.tone }}">
    <span class="batch-status__dot {{ meta.tone == 'running' ? 'batch-status__dot--pulse' }}"></span>
    <span>{{ meta.label }}</span>
</span>