{# Generated by GitHub Copilot #}
{% include 'admin/data_management/_styles.html.twig' %}

<style>
    .provider-hero-note {
        background: rgba(255, 255, 255, 0.12);
        border: 1px solid rgba(255, 255, 255, 0.24);
        border-radius: 1.25rem;
    }

    .provider-summary-card {
        background: #fff;
        border: 1px solid rgba(11, 60, 93, 0.08);
        border-radius: 1.35rem;
        box-shadow: 0 1rem 2rem rgba(11, 60, 93, 0.08);
    }

    .provider-summary-card__value {
        color: #0b3c5d;
        font-size: clamp(1.9rem, 3vw, 2.7rem);
        font-weight: 700;
        line-height: 1;
    }

    .provider-soft-badge {
        border-radius: 999px;
        display: inline-flex;
        font-size: 0.78rem;
        font-weight: 700;
        padding: 0.35rem 0.75rem;
    }

    .provider-soft-badge--success {
        background: rgba(25, 135, 84, 0.14);
        color: #146c43;
    }

    .provider-soft-badge--danger {
        background: rgba(220, 53, 69, 0.14);
        color: #b02a37;
    }

    .provider-soft-badge--warning {
        background: rgba(255, 193, 7, 0.2);
        color: #8f6b00;
    }

    .provider-soft-badge--info {
        background: rgba(13, 110, 253, 0.14);
        color: #0a58ca;
    }

    .provider-soft-badge--secondary {
        background: rgba(108, 117, 125, 0.14);
        color: #495057;
    }

    .provider-inline-list {
        display: grid;
        gap: 0.85rem;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    }

    .provider-inline-list article {
        background: #f7fafc;
        border: 1px solid rgba(11, 60, 93, 0.07);
        border-radius: 1rem;
        padding: 1rem;
    }

    .provider-inline-list p:first-child {
        color: #5b7080;
        font-size: 0.78rem;
        font-weight: 700;
        letter-spacing: 0.03em;
        margin-bottom: 0.35rem;
        text-transform: uppercase;
    }

    .provider-inline-list p:last-child {
        color: #0b3c5d;
        margin-bottom: 0;
    }

    .provider-resource-url {
        max-width: 340px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }
</style>