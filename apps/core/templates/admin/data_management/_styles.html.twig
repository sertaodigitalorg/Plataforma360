{# Generated by GitHub Copilot #}
<style>
    .data-management-hero {
        background: linear-gradient(135deg, #0b3c5d 0%, #13678a 48%, #f6fbfd 100%);
        border-radius: 1.75rem;
        color: #fff;
        overflow: hidden;
        position: relative;
    }

    .data-management-hero::after {
        background: radial-gradient(circle at top right, rgba(255, 255, 255, 0.28), transparent 38%);
        content: '';
        inset: 0;
        position: absolute;
    }

    .data-management-hero > * {
        position: relative;
        z-index: 1;
    }

    .data-management-card,
    .data-management-panel,
    .data-management-stat,
    .data-management-table-card {
        background: #fff;
        border: 1px solid rgba(11, 60, 93, 0.08);
        border-radius: 1.35rem;
        box-shadow: 0 1rem 2rem rgba(11, 60, 93, 0.08);
    }

    .data-management-card {
        height: 100%;
        transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
    }

    .data-management-card:hover {
        border-color: rgba(19, 103, 138, 0.2);
        box-shadow: 0 1.5rem 2.5rem rgba(11, 60, 93, 0.12);
        transform: translateY(-4px);
    }

    .data-management-icon,
    .data-management-stat__icon {
        align-items: center;
        background: #eef5f8;
        border-radius: 1rem;
        color: #0b3c5d;
        display: inline-flex;
        font-size: 1.25rem;
        height: 3rem;
        justify-content: center;
        width: 3rem;
    }

    .data-management-card__total {
        color: #0b3c5d;
        font-size: clamp(2rem, 3vw, 2.75rem);
        font-weight: 700;
        line-height: 1;
    }

    .data-management-quality-badge {
        border-radius: 999px;
        display: inline-flex;
        font-size: 0.75rem;
        font-weight: 600;
        padding: 0.3rem 0.7rem;
    }

    .data-management-quality-badge--success {
        background: rgba(25, 135, 84, 0.12);
        color: #146c43;
    }

    .data-management-quality-badge--warning {
        background: rgba(255, 193, 7, 0.16);
        color: #8f6b00;
    }

    .data-management-quality-badge--danger {
        background: rgba(220, 53, 69, 0.12);
        color: #b02a37;
    }

    .data-management-quality-badge--secondary {
        background: rgba(108, 117, 125, 0.14);
        color: #495057;
    }

    .data-management-quality-badge--primary {
        background: rgba(13, 110, 253, 0.12);
        color: #0a58ca;
    }

    .data-management-filter-pill {
        border-radius: 999px;
    }

    .data-management-detail-grid {
        display: grid;
        gap: 1rem;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    }

    .data-management-detail-item {
        background: #f8fbfc;
        border-radius: 1rem;
        padding: 1rem;
    }

    .data-management-detail-item dt {
        color: #5b7080;
        font-size: 0.82rem;
        font-weight: 600;
        margin-bottom: 0.35rem;
        text-transform: uppercase;
    }

    .data-management-detail-item dd {
        color: #0b3c5d;
        margin: 0;
        white-space: pre-wrap;
    }

    .data-management-form-card .form-label {
        font-weight: 600;
    }

    .data-management-table-card table td,
    .data-management-table-card table th {
        vertical-align: middle;
    }

    .data-management-table-card a:focus-visible,
    .data-management-panel a:focus-visible,
    .data-management-card a:focus-visible,
    .data-management-form-card button:focus-visible,
    .data-management-form-card a:focus-visible {
        outline: 3px solid #ffbf47;
        outline-offset: 2px;
    }
</style>