{# Generated by GitHub Copilot #}
{% set _route = app.current_route|default('') %}
{% set _path = app.request.pathInfo|default('/') %}
{% set intelligenceActive = _route == 'dashboard' or _path starts with '/dashboard' or _path starts with '/batch-monitor' or _path starts with '/analytics' or _path starts with '/observability' or _path starts with '/logs' or _path starts with '/indicators' or _path starts with '/admin/intelligence' %}
{% set dataActive = _path starts with '/admin/data' or _path starts with '/admin/tourist-spots' or _path starts with '/admin/events' or _path starts with '/admin/accommodations' or _path starts with '/admin/guides' or _path starts with '/admin/tourist-guides' or _path starts with '/admin/data-sources' or _path starts with '/exports' or _path starts with '/admin/analytics' %}
{% set integrationsActive = _path starts with '/public-api/v1' or _path starts with '/mcp/v1' or _path starts with '/api/docs' or _path starts with '/webhooks' or _path starts with '/integrations' or _path == '/api' or _path starts with '/admin/integrations' %}
{% set iaActive = _path starts with '/admin/ai' %}
{% set operationsActive = _path starts with '/admin/operations' %}
{% set governanceActive = _path starts with '/admin/governance' %}
{% set platformActive = _path starts with '/architecture' or _path starts with '/infrastructure' or _path starts with '/settings' or _path starts with '/users' or _path starts with '/security' or _path starts with '/about' or _path starts with '/admin/platform' %}
{% set userLabel = app.user ? (app.user.fullName is not empty ? app.user.fullName : app.user.userIdentifier) : 'Conta' %}

<style>
    .app-navbar {
        backdrop-filter: blur(14px);
        background: linear-gradient(135deg, rgba(7, 40, 68, 0.96) 0%, rgba(11, 60, 93, 0.94) 42%, rgba(14, 116, 144, 0.92) 100%);
        border-bottom: 1px solid rgba(255, 255, 255, 0.12);
        box-shadow: 0 1rem 2rem rgba(7, 40, 68, 0.18);
    }

    .app-navbar__container {
        padding-left: 20px;
        padding-right: 20px;
    }

    .app-navbar .navbar-brand {
        align-items: center;
        display: inline-flex;
        gap: 0.9rem;
        margin-right: 1.5rem;
        padding: 0;
    }

    .app-navbar__brand-mark {
        align-items: center;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.22), rgba(255, 255, 255, 0.08));
        border: 1px solid rgba(255, 255, 255, 0.18);
        border-radius: 1rem;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.14);
        color: #d8fffb;
        display: inline-flex;
        font-size: 1.1rem;
        height: 2.9rem;
        justify-content: center;
        width: 2.9rem;
    }

    .app-navbar__brand-title {
        color: #fff;
        display: block;
        font-size: 1rem;
        font-weight: 700;
        letter-spacing: 0.02em;
        line-height: 1.1;
    }

    .app-navbar__brand-subtitle {
        color: rgba(232, 245, 247, 0.82);
        display: block;
        font-size: 0.72rem;
        letter-spacing: 0.05em;
        text-transform: uppercase;
    }

    .app-navbar .navbar-toggler {
        border: 1px solid rgba(255, 255, 255, 0.18);
        border-radius: 0.95rem;
        box-shadow: none;
        padding: 0.55rem 0.75rem;
    }

    .app-navbar .navbar-toggler:focus {
        box-shadow: 0 0 0 0.2rem rgba(125, 232, 219, 0.28);
    }

    .app-navbar .navbar-collapse {
        margin-top: 0.9rem;
    }

    .app-navbar .navbar-nav {
        gap: 0.35rem;
    }

    .app-navbar .nav-link,
    .app-navbar .dropdown-toggle {
        align-items: center;
        border-radius: 0.9rem;
        color: rgba(245, 250, 252, 0.86);
        display: inline-flex;
        font-weight: 600;
        gap: 0.55rem;
        padding: 0.8rem 1rem;
        transition: background-color 0.2s ease, color 0.2s ease, transform 0.2s ease;
    }

    .app-navbar .nav-link:hover,
    .app-navbar .nav-link:focus,
    .app-navbar .dropdown-toggle:hover,
    .app-navbar .dropdown-toggle:focus,
    .app-navbar .nav-link.active,
    .app-navbar .show > .dropdown-toggle {
        background: rgba(125, 232, 219, 0.16);
        color: #fff;
        transform: translateY(-1px);
    }

    .app-navbar .dropdown-toggle::after {
        margin-left: 0.15rem;
    }

    .app-navbar .dropdown-menu {
        background: rgba(255, 255, 255, 0.98);
        border: 1px solid rgba(11, 60, 93, 0.08);
        border-radius: 1.15rem;
        box-shadow: 0 1.4rem 3rem rgba(7, 40, 68, 0.14);
        max-height: min(72vh, calc(100dvh - 7.5rem));
        min-width: 21rem;
        overflow-x: hidden;
        overflow-y: auto;
        overscroll-behavior: contain;
        padding: 0.75rem;
        scrollbar-gutter: stable;
        scrollbar-width: thin;
        scrollbar-color: rgba(11, 60, 93, 0.24) transparent;
    }

    .app-navbar .dropdown-menu::-webkit-scrollbar {
        width: 0.65rem;
    }

    .app-navbar .dropdown-menu::-webkit-scrollbar-track {
        background: transparent;
    }

    .app-navbar .dropdown-menu::-webkit-scrollbar-thumb {
        background: rgba(11, 60, 93, 0.22);
        border: 2px solid transparent;
        border-radius: 999px;
        background-clip: padding-box;
    }

    .app-navbar .dropdown-menu::-webkit-scrollbar-thumb:hover {
        background: rgba(11, 60, 93, 0.34);
        background-clip: padding-box;
    }

    .app-navbar .dropdown-header {
        color: #0b3c5d;
        font-size: 0.72rem;
        font-weight: 700;
        letter-spacing: 0.08em;
        padding: 0.45rem 0.75rem 0.65rem;
        text-transform: uppercase;
    }

    .app-navbar .dropdown-item {
        border-radius: 0.9rem;
        cursor: pointer;
        padding: 0.75rem;
        transition: background-color 0.2s ease, color 0.2s ease, opacity 0.2s ease, transform 0.2s ease;
    }

    .app-navbar .dropdown-item:hover,
    .app-navbar .dropdown-item:focus,
    .app-navbar .dropdown-item.active {
        background: rgba(15, 118, 110, 0.08);
        color: inherit;
        transform: translateX(2px);
    }

    .app-navbar .dropdown-item.disabled,
    .app-navbar .dropdown-item:disabled {
        opacity: 1;
    }

    .app-navbar__dropdown-item--operational .app-navbar__dropdown-icon {
        background: linear-gradient(135deg, #eff7fb 0%, #e4f7f4 100%);
    }

    .app-navbar__dropdown-item--operational:hover .app-navbar__dropdown-icon,
    .app-navbar__dropdown-item--operational:focus .app-navbar__dropdown-icon {
        background: linear-gradient(135deg, rgba(15, 118, 110, 0.14), rgba(45, 212, 191, 0.2));
        color: #0f766e;
    }

    .app-navbar__dropdown-item--planned {
        opacity: 0.8;
    }

    .app-navbar__dropdown-item--planned .app-navbar__dropdown-description {
        color: #728896;
    }

    .app-navbar__dropdown-item--planned .app-navbar__dropdown-icon {
        background: #f4f7f8;
        color: #6c7e8b;
    }

    .app-navbar .dropdown-item.active {
        background: rgba(20, 184, 166, 0.12);
        color: #0f766e;
    }

    .app-navbar .dropdown-item.active .app-navbar__dropdown-title,
    .app-navbar .dropdown-item.active .app-navbar__dropdown-description {
        color: #0f766e;
    }

    .app-navbar .dropdown-item.active .app-navbar__dropdown-icon {
        background: linear-gradient(135deg, rgba(15, 118, 110, 0.16), rgba(45, 212, 191, 0.24));
        color: #0f766e;
        box-shadow: inset 0 0 0 1px rgba(15, 118, 110, 0.08);
    }

    .app-navbar__dropdown-line {
        align-items: flex-start;
        display: flex;
        gap: 0.75rem;
        width: 100%;
    }

    .app-navbar__dropdown-icon {
        align-items: center;
        background: #eff7fb;
        border-radius: 0.8rem;
        color: #0f766e;
        display: inline-flex;
        font-size: 1rem;
        height: 2.1rem;
        justify-content: center;
        width: 2.1rem;
    }

    .app-navbar__dropdown-text {
        flex: 1 1 auto;
        min-width: 0;
    }

    .app-navbar__dropdown-title {
        color: #0b3c5d;
        display: flex;
        font-size: 0.96rem;
        font-weight: 700;
        gap: 0.5rem;
        justify-content: space-between;
        line-height: 1.2;
        margin-bottom: 0.25rem;
    }

    .app-navbar__dropdown-description {
        color: #5f7482;
        display: block;
        font-size: 0.82rem;
        line-height: 1.35;
        white-space: normal;
    }

    .app-navbar .badge {
        align-self: flex-start;
        border-radius: 999px;
        font-size: 0.68rem;
        font-weight: 700;
        letter-spacing: 0.03em;
        padding: 0.35rem 0.55rem;
    }

    .app-navbar .dropdown-divider {
        border-top-color: rgba(11, 60, 93, 0.08);
        margin: 0.55rem 0;
    }

    .app-navbar__account {
        align-items: center;
        display: flex;
        gap: 0.5rem;
    }

    .app-navbar__account .dropdown-toggle {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #fff;
        padding-left: 0.9rem;
        padding-right: 0.9rem;
    }

    .app-navbar__account .dropdown-toggle:hover,
    .app-navbar__account .dropdown-toggle:focus,
    .app-navbar__account .show > .dropdown-toggle {
        background: rgba(125, 232, 219, 0.18);
        color: #fff;
    }

    .app-navbar__account-name {
        display: inline-flex;
        flex-direction: column;
        line-height: 1.1;
        text-align: left;
    }

    .app-navbar__account-name strong {
        font-size: 0.92rem;
        font-weight: 700;
    }

    .app-navbar__account-name small {
        color: rgba(232, 245, 247, 0.78);
        font-size: 0.7rem;
        letter-spacing: 0.04em;
        text-transform: uppercase;
    }

    .app-navbar__account-menu {
        min-width: 15rem;
    }

    .app-navbar__account-menu .dropdown-item i {
        margin-right: 0.5rem;
        width: 1rem;
    }

    .app-navbar__login-link {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 0.9rem;
        color: #fff;
        display: inline-flex;
        font-weight: 600;
        gap: 0.5rem;
        padding: 0.8rem 1rem;
        text-decoration: none;
        transition: background-color 0.2s ease, color 0.2s ease;
    }

    .app-navbar__login-link:hover,
    .app-navbar__login-link:focus {
        background: rgba(125, 232, 219, 0.18);
        color: #fff;
    }

    @media (min-width: 992px) {
        .app-navbar .navbar-collapse {
            margin-top: 0;
        }

        .app-navbar .dropdown-menu {
            margin-top: 0.85rem;
        }
    }

    @media (max-width: 991.98px) {
        .app-navbar .navbar-collapse {
            background: rgba(7, 40, 68, 0.9);
            border: 1px solid rgba(255, 255, 255, 0.12);
            border-radius: 1.25rem;
            margin-top: 1rem;
            padding: 0.9rem;
        }

        .app-navbar .dropdown-menu {
            background: rgba(255, 255, 255, 0.96);
            box-shadow: none;
            max-height: min(55vh, calc(100dvh - 12rem));
            margin-top: 0.45rem;
            min-width: 100%;
        }

        .app-navbar__container {
            padding-left: 20px;
            padding-right: 20px;
        }

        .app-navbar__account {
            border-top: 1px solid rgba(255, 255, 255, 0.12);
            margin-top: 0.9rem;
            padding-top: 0.9rem;
        }
    }
</style>

{% macro dropdown_item(label, description, icon, href, active, badge, badgeTone, disabled, planned) %}
    <li>
        <a class="dropdown-item {{ active ? 'active' }} {{ disabled ? 'disabled' }} {{ planned or badge ? 'app-navbar__dropdown-item--planned' : 'app-navbar__dropdown-item--operational' }}" href="{{ href }}" {% if disabled %}aria-disabled="true"{% endif %}>
            <span class="app-navbar__dropdown-line">
                <span class="app-navbar__dropdown-icon"><i class="bi bi-{{ icon }}"></i></span>
                <span class="app-navbar__dropdown-text">
                    <span class="app-navbar__dropdown-title">
                        <span>{{ label }}</span>
                        {% if badge %}
                            <span class="badge text-bg-{{ badgeTone|default('secondary') }}">{{ badge }}</span>
                        {% endif %}
                    </span>
                    <span class="app-navbar__dropdown-description">{{ description }}</span>
                </span>
            </span>
        </a>
    </li>
{% endmacro %}

{% import _self as navbar %}

<nav class="navbar navbar-expand-lg fixed-top app-navbar">
    <div class="container-fluid app-navbar__container">
        <a class="navbar-brand" href="{{ path('homepage') }}" aria-label="Olinda360 - Plataforma de Inteligência Territorial">
            <span class="app-navbar__brand-mark"><i class="bi bi-buildings"></i></span>
            <span>
                <span class="app-navbar__brand-title">Olinda360</span>
                <span class="app-navbar__brand-subtitle">GovTech & Turismo Inteligente</span>
            </span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#appNavbar" aria-controls="appNavbar" aria-expanded="false" aria-label="Abrir navegação principal">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="appNavbar">
            <ul class="navbar-nav align-items-lg-center ms-lg-4 me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ _route == 'homepage' ? 'active' }}" href="{{ path('homepage') }}">
                        <i class="bi bi-house-door"></i>
                        <span>Início</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ intelligenceActive ? 'active' }}" href="{{ path('app_admin_intelligence_hub') }}">
                        <i class="bi bi-bar-chart-line"></i>
                        <span>Inteligência</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ dataActive ? 'active' }}" href="{{ path('app_admin_data_hub') }}">
                        <i class="bi bi-database"></i>
                        <span>Dados</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ integrationsActive ? 'active' }}" href="{{ path('app_admin_integrations_hub') }}">
                        <i class="bi bi-plug"></i>
                        <span>Integrações</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ iaActive ? 'active' }}" href="{{ path('app_admin_ai_hub') }}">
                        <i class="bi bi-robot"></i>
                        <span>IA</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ operationsActive ? 'active' }}" href="{{ path('app_admin_operations_hub') }}">
                        <i class="bi bi-diagram-3"></i>
                        <span>Operações</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ governanceActive ? 'active' }}" href="{{ path('app_admin_governance_hub') }}">
                        <i class="bi bi-shield-check"></i>
                        <span>Governança</span>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{ platformActive ? 'active' }}" href="{{ path('app_admin_platform_hub') }}">
                        <i class="bi bi-layers"></i>
                        <span>Plataforma</span>
                    </a>
                </li>
            </ul>

            <div class="app-navbar__account ms-lg-3">
                {% if app.user %}
                    <div class="dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle"></i>
                            <span class="app-navbar__account-name">
                                <strong>{{ userLabel }}</strong>
                                <small>Perfil</small>
                            </span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end app-navbar__account-menu">
                            <li>
                                <a class="dropdown-item" href="{{ path('user_edit') }}">
                                    <i class="bi bi-person"></i>Meu perfil
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('user_change_password') }}">
                                    <i class="bi bi-key"></i>Alterar senha
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ logout_path('main') }}">
                                    <i class="bi bi-box-arrow-right"></i>Sair
                                </a>
                            </li>
                        </ul>
                    </div>
                {% else %}
                    <a class="app-navbar__login-link" href="{{ path('security_login') }}">
                        <i class="bi bi-box-arrow-in-right"></i>
                        <span>Login</span>
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
