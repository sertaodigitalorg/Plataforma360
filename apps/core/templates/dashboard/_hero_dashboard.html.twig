{# Generated by GitHub Copilot #}
<div class="dashboard-hero p-4 p-lg-5 mb-4">
    <div class="dashboard-hero__content row g-4 align-items-end">
        <div class="col-lg-8">
            <span class="badge rounded-pill text-bg-light text-primary mb-3">{{ hero.badge }}</span>
            <h1 id="dashboard-title" class="display-6 fw-bold mb-3">{{ hero.title }}</h1>
            <p class="lead mb-0">{{ hero.subtitle }}</p>
        </div>
        <div class="col-lg-4">
            <div class="dashboard-glass-card rounded-4 p-4 h-100">
                <p class="text-uppercase small fw-semibold mb-2">Indicador operacional</p>
                <p class="fs-2 fw-bold mb-1">{{ hero.uptime }}</p>
                <p class="mb-3 text-white-50">{{ hero.uptimeDescription }}</p>
                <div class="d-flex justify-content-between gap-3 flex-wrap small">
                    <div>
                        <span class="d-block text-white-50">Registros monitorados</span>
                        <strong>{{ hero.totalRecords }}</strong>
                    </div>
                    <div>
                        <span class="d-block text-white-50">Fontes estratégicas</span>
                        <strong>{{ hero.sourcesMonitored }}</strong>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>